import styled from 'styled-components';

export const ListWrapper = styled.div`
  .ant-tabs-content-holder {
    height: 100%;
  }

  .table-content {
    .vActions {
      .ant-btn {
        position: absolute;
        top: 10px;
        right: 10px;
        height: auto;
        padding: 3px 25px;
      }
    }
  }
`;
